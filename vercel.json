{
  "framework": null,
  "buildCommand": "cd packages/shared && pnpm build && cd ../.. && npx vite build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
